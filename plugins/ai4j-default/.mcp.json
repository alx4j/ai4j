{
  "mcpServers": {
    "claude-tools": {
      "type": "stdio",
      "command": "claude",
      "args": [
        "mcp",
        "serve"
      ],
      "env": {
        "AI4J_TOKEN": "${AI4J_TOKEN}"
      }
    }
  }
}
