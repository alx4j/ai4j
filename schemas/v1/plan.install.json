{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/alx4j/ai4j/schemas/v1/plan.install.json",
  "title": "AI4J schema v1 plan.install response",
  "$defs": {
    "data": {
      "allOf": [
        {"$ref": "common.json#/$defs/planData"},
        {"properties": {"operation": {"const": "install"}}}
      ]
    }
  },
  "allOf": [
    {"$ref": "common.json#/$defs/readOnlyEnvelope"},
    {"$ref": "common.json#/$defs/planEnvelopeSemantics"},
    {"$ref": "common.json#/$defs/updateDispositionEnvelopeSemantics"},
    {
      "type": "object",
      "properties": {
        "command": {"const": "plan.install"},
        "data": {
          "oneOf": [
            {"$ref": "#/$defs/data"},
            {"type": "null"}
          ]
        }
      },
      "allOf": [
        {
          "if": {
            "required": ["status"],
            "properties": {"status": {"enum": ["ok", "no_change", "degraded"]}}
          },
          "then": {"properties": {"data": {"$ref": "#/$defs/data"}}}
        },
        {
          "if": {"required": ["data"], "properties": {"data": {"type": "null"}}},
          "then": {"properties": {"status": {"enum": ["cancelled", "error"]}}}
        }
      ],
      "additionalProperties": true
    }
  ]
}
