// Package pathsafe defines target-, host-, and source-neutral lexical path
// contracts. Its values carry no filesystem authority; host adapters must still
// resolve them relative to an already-qualified root.
package pathsafe
