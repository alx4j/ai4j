# AI4J shared rules

Deliver the smallest complete vertical slice that meets the explicit acceptance criteria. Keep optional hardening and speculative abstractions out of the active task. Never expose credentials, execute toolkit content during lifecycle operations, or overwrite content that AI4J does not own.
